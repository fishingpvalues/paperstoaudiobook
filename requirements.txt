# PDF to Audiobook Converter - Requirements
# Install with: pip install -r requirements.txt

# PDF processing
PyPDF2>=3.0.0

# Audio processing
pydub>=0.25.1

# Progress bars (user-friendly visual feedback)
tqdm>=4.66.0

# Text-to-Speech (Kokoro - audiobook quality, CPU-only)
kokoro-onnx>=0.1.0
kokoro
# OCR Support (for scanned PDFs and images)
# PaddleOCR - Advanced CPU-friendly OCR (no system dependencies needed!)
paddleocr>=2.7.0
paddlepaddle>=2.6.0

# Image processing
Pillow>=10.0.0
pdf2image>=1.16.3

# Optional: Tesseract OCR as fallback (requires system installation)
# pytesseract>=0.3.10
# Note: For Tesseract, you also need to install it on your system:
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# Mac: brew install tesseract
# Linux: sudo apt-get install tesseract-ocr
